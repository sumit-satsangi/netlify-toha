# section information
section:
  name: Recent Posts
  id: recent-posts
  enable: true
  weight: 0
  showOnNavbar: true
  # Can optionally hide the title in sections
  hideTitle: false

# no additional configuration is required
